---
description: Show the change as a unified diff — don't apply it
argument-hint: [change]
disable-model-invocation: true
---
Show the change as a unified diff (`---`/`+++`/`@@` format) in one code block. Do NOT edit any files — I'll apply it myself. No explanation beyond one line above the diff. Reading files to build an accurate diff is fine.

Change: $ARGUMENTS
