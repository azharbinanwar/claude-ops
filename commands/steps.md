---
description: Numbered how-to steps only, no prose between
argument-hint: [what you want to do]
model: haiku
disable-model-invocation: true
---
Give numbered steps only. One line per step; a command in backticks where a step is a command. No intro, no prose between steps, no closing summary. Max 10 steps.

How to: $ARGUMENTS
