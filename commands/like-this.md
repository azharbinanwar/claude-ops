---
description: Build it by mirroring an existing pattern — no redesign
argument-hint: [new thing] like [existing thing, e.g. JuzTab like SurahTab]
disable-model-invocation: true
---
Build the new thing by mirroring the existing reference implementation named below. Read only the reference and the files you must touch — the reference IS the design, do not rethink it.

Copy its structure, naming style, wiring, and error handling exactly, adapted to the new case. No improvements, no refactors of the reference, no alternative approaches. If the pattern genuinely cannot apply somewhere, say so in one line and do the closest thing.

Task: $ARGUMENTS
