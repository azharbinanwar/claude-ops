---
description: Follow project conventions exactly — no going out of the box
argument-hint: [task]
disable-model-invocation: true
---
Do this task strictly inside the project's existing conventions:

- Check CLAUDE.md / README / docs for stated rules before starting, and obey them.
- Match the existing folder structure, naming, architecture layers, and patterns — put things where this project puts them, not where you'd put them.
- No new dependencies, no new patterns, no restructuring, nothing "creative".
- If the task seems to require breaking a convention, STOP and say so in one line instead of improvising.

Task: $ARGUMENTS
