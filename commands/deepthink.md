---
description: Opposite of /short — max reasoning, full exploration
argument-hint: [hard problem]
model: opus
disable-model-invocation: true
---
ultrathink. This one is worth the tokens: reason exhaustively before answering. Explore the relevant code, consider edge cases and at least one alternative you then reject with a reason. Take the time a hard problem deserves — but the final answer should still be structured and readable, not a dump.

Problem: $ARGUMENTS
