---
description: Fresh-eyes second opinion before committing to an approach
argument-hint: [approach / plan / diff to review]
disable-model-invocation: true
---
Use the Agent tool to spawn the `consult` agent with everything it needs to judge this independently: the proposal below plus the relevant context (files, constraints, what was already tried). Then report back: its verdict verbatim (condensed), followed by one line — whether you agree and why.

Proposal: $ARGUMENTS
